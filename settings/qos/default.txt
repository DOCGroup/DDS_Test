# most often changed QoS
presentation=ordered_access
durability=volatile
history=keep_last
reliability=best_effort

# resource limits
samples.max=100000
samples.max_per_instance=10000
instances.max=50

# transport type (multicast/unicast)
transport=unicast

# protocol (UDP/TCP)
protocol=UDP

