// -*- MPC -*-
// $Id$

project {
  Define_Custom(SpliceTypeSupport) {
    automatic             = 1
    command               = <%quote%>$(OSPL_HOME)/bin/idlpp<%quote%>
    dependent             = <%quote%>$(OSPL_HOME)/bin/idlpp<%quote%>
    source_outputext      = SacDcps.c
    keyword splice_ts_flags = commandflags
  }

  splice_ts_flags += -l c
}
